modelVersion: 4.0.0

parent:
  groupId: org.lambdify
  artifactId: lambdify-aws-events-parent
  version: 0.8.0.Final
  relativePath: "../pom.yml"

groupId: org.lambdify
artifactId: lambdify-aws-events-kinesis
packaging: jar

properties:
  version.lombok: 1.18.6

scm: { url: "git@github.com:lambdify/lambdify.git" }

dependencies:
 - { groupId: com.fasterxml.jackson.core, artifactId: jackson-annotations, version: "${version.jackson}", scope: "provided" }
 - { groupId: com.jsoniter, artifactId: jsoniter, version: "0.9.19", scope: "test" }

